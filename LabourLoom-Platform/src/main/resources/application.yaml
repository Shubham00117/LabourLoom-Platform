spring:
  datasource:
    url: jdbc:mysql://localhost:3306/labourloom_platform_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: biysiww1179881
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false

server:
  port: 8080 

servlet:
  multipart:
    max-file-size: 10MB
    max-request-size: 11MB
   
file:
  upload:
    location: Images/Workers/
  upload2:
    location: Images/Customers/
